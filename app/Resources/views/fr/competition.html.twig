{% extends 'FOLayout.html.twig' %}

{% block title %}
  {% for competition in competitions %}
      {{ competition.titre }}
  {% endfor %}
{% endblock %}

{% block meta %}
  {% for competition in competitions %}
  <meta name="keywords" content="{{ competition.tags }}"  />
  <meta name="description" content="{{ competition.resume }}">
  <meta property="og:locale" content="fr_FR" />
    <meta property="og:locale:alternate" content="en_US" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content="{{ competition.titre }}" />
    <meta property="og:description" content="{{ competition.resume }}" />
    <meta property="og:url" content="http://reseau-nzassa.org/competition/{{ competition.slug }}" />
    <meta property="og:image" content="http://www.team-stl.com{{ asset('ressources/images/logo.jpg') }}" />
    <meta property="og:site_name" content="S.A. TEAM STL" />
    <meta name="twitter:card" content="summary"/>
    <meta name="twitter:description" content="{{ competition.resume }}"/>
    <meta name="twitter:title" content="{{ competition.titre }}"/>
    <meta name="twitter:domain" content="S.A. TEAM STL"/>
    <meta property="twitter:image" content="http://www.team-stl.com{{ asset('ressources/images/logo.jpg') }}" />
  {% endfor %}
{% endblock %}

{% block body %}

{% for competition in competitions %}
<!-- colonne principale -->
     <div class="medium-8 columns">
         <!-- breadcrumbs -->
       <nav aria-label="You are here:" role="navigation">
         <ul class="breadcrumbs">
           <li><a href="{{ path('homepage') }}"><i class="fa fa-home fa-lg" aria-hidden="true"></i></a></li>
           <!--<li><a href="#">Features</a></li>-->
           <li class="disabled">competition</li>
           <li>
             <span class="show-for-sr">Current: </span> {{ competition.rubrique }}
           </li>
         </ul>
       </nav>
       <!-- \breadcrumbs -->
       <div class="row">
         <div class="columns content">
           <div class="titre">{{ competition.titre }} </div>
           {% if competition.image %}
               <img src="{{ asset('ressources/images/competition/' ~ competition.image.id ~ '.' ~ competition.image.url )}}" alt="{{ competition.titre }}" class="thumbnail" />
           {% else %}
               <img src="{{ asset('ressources/images/logo.jpg')}}" alt="{{ competition.titre }}" class="thumbnail" />
           {% endif %}
             <p>
               {{ competition.contenu|raw }}
             </p>

         </div>
       </div>
     </div>
     <!-- \colonne principale -->
{% endfor %}

{% endblock %}
