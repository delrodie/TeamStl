{% extends 'base.html.twig' %}

{% block body %}
    <h1>Imgmembre</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imgMembre.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ imgMembre.url }}</td>
            </tr>
            <tr>
                <th>Alt</th>
                <td>{{ imgMembre.alt }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{% if imgMembre.statut %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_imgmembre_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_imgmembre_edit', { 'id': imgMembre.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
