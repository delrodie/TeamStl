{% extends 'base.html.twig' %}

{% block body %}
    <h1>Imgcompetition</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imgCompetition.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ imgCompetition.url }}</td>
            </tr>
            <tr>
                <th>Alt</th>
                <td>{{ imgCompetition.alt }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{% if imgCompetition.statut %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_imgcompetition_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_imgcompetition_edit', { 'id': imgCompetition.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
