{% extends 'base.html.twig' %}

{% block body %}
    <h1>Imginitiation</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imgInitiation.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ imgInitiation.url }}</td>
            </tr>
            <tr>
                <th>Alt</th>
                <td>{{ imgInitiation.alt }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{% if imgInitiation.statut %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_imginitiation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_imginitiation_edit', { 'id': imgInitiation.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
